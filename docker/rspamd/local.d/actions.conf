# Spam score thresholds. Tune to taste; these are conservative defaults.
#   greylist   -> temporarily defer (retry proves a real MTA)
#   add_header -> deliver, but tag as spam (X-Spam headers)
#   reject     -> refuse outright
greylist = 4;
add_header = 6;
reject = 15;
