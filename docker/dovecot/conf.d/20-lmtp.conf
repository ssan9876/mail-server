# ============================================================================
# LMTP delivery — auto-create standard folders, enforce quota on delivery.
# postmaster_address is set at runtime in 99-runtime.conf.
# ============================================================================

protocol lmtp {
  mail_plugins = $mail_plugins quota
}

lda_mailbox_autocreate = yes
lda_mailbox_autosubscribe = yes
