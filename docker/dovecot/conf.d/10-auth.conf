# ============================================================================
# Authentication — SQL-backed, TLS required before any plaintext mechanism.
# ============================================================================

# Force STARTTLS/implicit TLS before credentials are accepted.
disable_plaintext_auth = yes
auth_mechanisms = plain login

# Usernames are full email addresses (local_part@domain).
auth_username_format = %Lu

!include auth-sql.conf.ext
